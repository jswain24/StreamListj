Header.js  => to display the title Streamlist

import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>StreamList</h1>
        </header>
    );
};

export default Header;
